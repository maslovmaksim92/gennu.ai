import { HttpClient } from '@angular/common/http'; import { Component, inject, signal } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { InlineAiComponent } from '../shared/inline-ai.component'; import { AgGridAngular } from 'ag-grid-angular'; import { AllCommunityModule, ColDef, ModuleRegistry } from 'ag-grid-community'; ModuleRegistry.registerModules([AllCommunityModule]); @Component({standalone:true,imports:[FormsModule,AgGridAngular,InlineAiComponent],template:`<section class="page"><header class="page-head"><div><span class="eyebrow">DESIGN SYSTEM</span><h1>Blocks</h1><p>Reusable system block definitions.</p></div><button class="primary" (click)="creating.set(!creating())">New block</button></header>@if(creating()){<div class="design-editor"><div class="panel editor"><label>Key<input [(ngModel)]="key"></label><label>Name<input [(ngModel)]="name"></label><label>Block schema JSON<textarea [(ngModel)]="schema" rows="16"></textarea></label><button class="primary" (click)="create()">Save draft</button></div><proto-inline-ai title="Block assistant" context="You are helping an administrator define a reusable Proto.ai UI block. Keep data and settings separate, use a portable schema, variants and semantic theme tokens. Do not generate framework-specific code." placeholder="Например: создай schema для Hero с split и centered variants…" /></div>}<div class="panel grid-panel"><ag-grid-angular style="width:100%;height:480px" [rowData]="rows()" [columnDefs]="cols" /></div></section>`}) export class BlocksComponent {http=inject(HttpClient);rows=signal<any[]>([]);creating=signal(false);key='';name='';schema='{"type":"object","properties":{"data":{"type":"object"},"settings":{"type":"object"}}}';cols:ColDef[]=[{field:'name',flex:1},{field:'key',flex:1},{field:'version',width:100},{field:'status',width:140},{field:'updatedAt',headerName:'Updated',flex:1}];constructor(){this.load()}load(){this.http.get<any[]>('/api/blocks').subscribe(x=>this.rows.set(x))}create(){let parsed={};try{parsed=JSON.parse(this.schema)}catch{return}this.http.post('/api/blocks',{key:this.key,name:this.name,schema:parsed}).subscribe(()=>{this.creating.set(false);this.key='';this.name='';this.load()})}}