import { HttpClient } from '@angular/common/http'; import { Component, inject, signal } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { InlineAiComponent } from '../shared/inline-ai.component'; import { AgGridAngular } from 'ag-grid-angular'; import { AllCommunityModule, ColDef, ModuleRegistry } from 'ag-grid-community'; ModuleRegistry.registerModules([AllCommunityModule]); @Component({standalone:true,imports:[FormsModule,AgGridAngular,InlineAiComponent],template:`<section class="page"><header class="page-head"><div><span class="eyebrow">DESIGN SYSTEM</span><h1>Themes</h1><p>System themes available to future Studio projects.</p></div><button class="primary" (click)="creating.set(!creating())">New theme</button></header>@if(creating()){<div class="design-editor"><div class="panel editor"><label>Key<input [(ngModel)]="key"></label><label>Name<input [(ngModel)]="name"></label><label>Theme JSON<textarea [(ngModel)]="schema" rows="16"></textarea></label><button class="primary" (click)="create()">Save draft</button></div><proto-inline-ai title="Theme assistant" context="You are helping an administrator create a Proto.ai Theme Schema. Suggest structured JSON using semantic design tokens. Do not generate arbitrary CSS." placeholder="Например: сделай премиальную светлую тему с serif-заголовками…" /></div>}<div class="panel grid-panel"><ag-grid-angular style="width:100%;height:480px" [rowData]="rows()" [columnDefs]="cols" /></div></section>`}) export class ThemesComponent {http=inject(HttpClient);rows=signal<any[]>([]);creating=signal(false);key='';name='';schema='{"colors":{},"typography":{},"spacing":{},"radius":{}}';cols:ColDef[]=[{field:'name',flex:1},{field:'key',flex:1},{field:'version',width:100},{field:'status',width:140},{field:'updatedAt',headerName:'Updated',flex:1}];constructor(){this.load()}load(){this.http.get<any[]>('/api/themes').subscribe(x=>this.rows.set(x))}create(){let parsed={};try{parsed=JSON.parse(this.schema)}catch{return}this.http.post('/api/themes',{key:this.key,name:this.name,schema:parsed}).subscribe(()=>{this.creating.set(false);this.key='';this.name='';this.load()})}}